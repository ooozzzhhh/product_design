# 财务管理 · 测试用例

本文档基于《财务管理-设计说明.md》与原型 `财务管理.html` 编写，覆盖「财务版本」「售价明细」「成本明细」三个子模块的全部用户操作与业务规则，供测试执行使用。

---

## 一、文档说明

| 项目 | 说明 |
|------|------|
| 适用模块 | 财务版本与成本管理（财务版本、售价明细、成本明细） |
| 用例编写依据 | 设计说明 + 产品原型图 |
| 用例目标 | 全面覆盖所有可能的用户操作、边界条件与异常场景 |
| 优先级 | P0=核心必测，P1=重要，P2=一般/边界 |

---

## 二、页面与导航

### 2.1 一级主标签与页面结构

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| NAV-001 | 主标签切换 | 点击顶部「财务版本」 | 主内容区显示财务版本列表与顶栏（筛选、新增版本、导入、导出、视图设置、数据校验） | P0 |
| NAV-002 | 主标签切换 | 点击顶部「售价明细」 | 主内容区显示售价明细：财务版本下拉、未保存提示区、初始化/保存/导入/导出/视图设置；筛选区与售价表格 | P0 |
| NAV-003 | 主标签切换 | 点击顶部「成本明细」 | 主内容区显示成本明细：财务版本下拉、未保存提示区、初始化/保存/导入/导出/视图设置；筛选区与成本类型子标签（生产/采购/运输/库存/违约罚金） | P0 |
| NAV-004 | 当前主标签高亮 | 在任意主标签下 | 当前主标签有主题色下划线与字重区分，其余为默认样式 | P2 |
| NAV-005 | 面包屑与标题 | 进入页面 | 顶部显示「财务管理」及副标题「财务版本 · 售价明细 · 成本明细」 | P2 |

---

## 三、财务版本

### 3.1 筛选

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| VER-F01 | 筛选面板展开 | 点击顶栏「筛选」 | 筛选面板展开，显示：版本名称、版本状态、开始日期、结束日期、查询、重置 | P0 |
| VER-F02 | 筛选面板收起 | 再次点击「筛选」 | 筛选面板收起 | P0 |
| VER-F03 | 按版本名称查询 | 输入版本名称（全匹配或部分），点击「查询」 | 列表仅显示版本名称符合条件的记录 | P0 |
| VER-F04 | 按版本状态查询 | 版本状态下拉选择「创建」/「审批中」/「已发布」，点击「查询」 | 列表仅显示该状态的记录 | P0 |
| VER-F05 | 按日期范围查询 | 选择开始日期、结束日期，点击「查询」 | 列表仅显示时段在范围内的版本 | P0 |
| VER-F06 | 组合条件查询 | 同时填写多个筛选条件后点击「查询」 | 列表为各条件交集结果 | P0 |
| VER-F07 | 重置 | 填写任意筛选条件后点击「重置」 | 筛选条件清空，列表恢复为重置前数据范围（或默认全部） | P0 |
| VER-F08 | 空条件查询 | 不填任何条件直接点击「查询」 | 列表显示全部数据（或与未筛选一致） | P1 |

### 3.2 新增 / 编辑版本

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| VER-A01 | 打开新增弹窗 | 点击「新增版本」 | 弹出「新增财务版本」弹窗，版本名称为必填，版本状态、开始/结束日期、备注可填；有「复制现有版本」选项 | P0 |
| VER-A02 | 必填校验 | 不填版本名称，点击「保存」 | 提示版本名称为必填，不提交 | P0 |
| VER-A03 | 新增保存成功 | 填写版本名称（必填）及可选字段，点击「保存」 | 弹窗关闭，列表刷新并包含新记录；可选：售价/成本模块的「财务版本」下拉数据源更新 | P0 |
| VER-A04 | 取消新增 | 填写部分内容后点击「取消」或关闭按钮 | 弹窗关闭，无数据变更 | P0 |
| VER-A05 | 复制现有版本 | 勾选「复制现有版本」，选择源版本，填写新版本名称后保存 | 按设计逻辑复制源版本数据生成新版本，列表中出现新记录 | P1 |
| VER-A06 | 打开编辑弹窗 | 点击某行「编辑」 | 弹出编辑弹窗，标题为「编辑财务版本」，表单回填该行数据 | P0 |
| VER-A07 | 编辑保存成功 | 修改版本名称/状态/日期/备注后点击「保存」 | 弹窗关闭，列表刷新且该行显示新数据 | P0 |
| VER-A08 | 编辑取消 | 修改后点击「取消」或关闭 | 弹窗关闭，列表数据不变 | P0 |
| VER-A09 | 开始日期晚于结束日期 | 开始日期 > 结束日期 保存 | 根据实现：提示错误并禁止保存，或后端校验返回错误 | P1 |

### 3.3 删除版本

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| VER-D01 | 删除确认 | 点击某行「删除」 | 弹出二次确认（如「确定删除该财务版本？」） | P0 |
| VER-D02 | 确认删除 | 在确认弹窗中确认 | 该版本从列表移除，提示删除成功 | P0 |
| VER-D03 | 取消删除 | 在确认弹窗中取消 | 不删除，列表不变 | P0 |

### 3.4 导入

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| VER-I01 | 打开导入弹窗 | 点击「导入」 | 弹出「导入财务版本」弹窗：导入方式（全量/增量）、文件选择（.xlsx/.xls/.csv）、取消、确认导入 | P0 |
| VER-I02 | 全量导入 | 选择「全量导入」，选择合法文件，点击「确认导入」 | 清空现有数据后导入文件内容，列表刷新为导入结果，提示成功 | P0 |
| VER-I03 | 增量导入 | 选择「增量导入」，选择合法文件，点击「确认导入」 | 在现有数据基础上追加导入，列表刷新，提示成功 | P0 |
| VER-I04 | 未选文件 | 不选文件直接「确认导入」 | 提示请选择文件或类似错误，不执行导入 | P0 |
| VER-I05 | 非法格式 | 选择非 .xlsx/.xls/.csv 文件 | 根据实现：文件选择器限制或提交时提示格式不支持 | P1 |
| VER-I06 | 取消导入 | 点击「取消」或关闭 | 弹窗关闭，无数据变更 | P0 |

### 3.5 导出

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| VER-E01 | 导出当前列表 | 在筛选后的列表下点击「导出」 | 导出当前列表（含筛选结果）为文件（如 Excel/CSV），文件名或内容与当前页一致 | P0 |
| VER-E02 | 无数据导出 | 列表无数据时点击「导出」 | 根据实现：禁用按钮或提示无数据可导出 | P1 |

### 3.6 数据校验

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| VER-V01 | 打开校验结果 | 选择版本后点击「数据校验」（若启用） | 弹出「数据校验结果」弹窗，展示校验摘要；可下载详细结果 | P1 |
| VER-V02 | 下载校验结果 | 在校验结果弹窗中点击「下载详细结果」 | 下载校验详细结果文件 | P1 |

### 3.7 视图设置

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| VER-VS01 | 打开视图设置 | 点击「视图设置」 | 弹出「财务版本视图设置」弹窗，含三个 Tab：筛选、排序、列设置 | P0 |
| VER-VS02 | 高级筛选规则 | 在「筛选」Tab 中添加规则（字段、运算符、值类型、值、且/或），支持增删、拖拽排序 | 规则列表正确展示；保存后再次打开列表，先应用顶栏快捷筛选再应用此处筛选 | P0 |
| VER-VS03 | 排序规则 | 在「排序」Tab 中添加多规则（字段、升/降序），增删、拖拽排序 | 保存后列表按该排序规则展示 | P0 |
| VER-VS04 | 列设置 | 在「列设置」Tab 中拖拽调整列顺序、设置列宽、显示/隐藏列 | 保存后表格列顺序、列宽、显隐与设置一致 | P0 |
| VER-VS05 | 保存视图设置 | 修改任意 Tab 后点击「保存设置」 | 弹窗关闭，列表按新设置展示；下次进入页面恢复该设置 | P0 |
| VER-VS06 | 取消视图设置 | 修改后点击「取消」或关闭 | 弹窗关闭，列表设置不变 | P0 |

### 3.8 表格与多选

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| VER-T01 | 表头全选 | 点击表头勾选框 | 当前页所有行勾选 | P1 |
| VER-T02 | 单行勾选 | 点击某行勾选框 | 该行勾选/取消；设计上多选预留，无批量操作时仅影响勾选状态 | P1 |
| VER-T03 | 表格滚动 | 数据过多时纵向滚动表格 | 表头固定，内容区可滚动，最大高度内显示正常 | P1 |

---

## 四、售价明细

### 4.1 财务版本与顶栏

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| PRC-V01 | 选择财务版本 | 在「财务版本」下拉中选择一个版本 | 列表立即按该版本筛选并刷新，无需切换主标签 | P0 |
| PRC-V02 | 未选版本 | 不选版本或选「请选择版本」 | 列表为空或提示请选择版本 | P0 |
| PRC-V03 | 保存按钮默认 | 无任何修改时 | 「保存」按钮禁用 | P0 |
| PRC-V04 | 有待保存修改 | 行内或批量编辑后 | 顶栏显示「*」「（更改待保存）」；「保存」按钮高亮可点 | P0 |
| PRC-V05 | 保存成功 | 有修改时点击「保存」 | 数据提交成功，待保存标记清除，列表刷新 | P0 |

### 4.2 筛选与展开/折叠

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| PRC-F01 | 筛选面板 | 点击「筛选」 | 筛选面板展开：库存点代码、物品代码、销售部门ID、客户ID、时间范围、查询、重置 | P0 |
| PRC-F02 | 快捷筛选 | 填写筛选条件后点击「查询」 | 列表仅显示符合条件的数据 | P0 |
| PRC-F03 | 重置筛选 | 点击「重置」 | 筛选条件清空，列表恢复 | P0 |
| PRC-F04 | 全部展开 | 点击「全部展开」 | 当前页所有分组父行处于展开状态 | P0 |
| PRC-F05 | 全部折叠 | 点击「全部折叠」 | 当前页所有分组父行处于折叠状态 | P0 |
| PRC-F06 | 父行展开/折叠 | 点击某父行（组头） | 该组展开或折叠；展开时先显示组内平均行，再显示子行 | P0 |
| PRC-F07 | 点击父行复选框 | 点击父行前的复选框 | 仅勾选/取消该组全部子行，不触发展开/折叠 | P0 |

### 4.3 分组与分页

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| PRC-G01 | 分组规则 | 查看列表 | 按「库存点+物品+版本+客户」分组；父行展示组键及子行条数 | P0 |
| PRC-G02 | 组内平均行 | 展开某组 | 组内平均行显示基础售价、折扣率、最终售价的组内平均值，保留两位小数 | P0 |
| PRC-G03 | 分页按组 | 切换每页条数（10/20/50）或页码 | 分页按父行（组）数量分页；底部显示「共 N 组」及分页控件 | P0 |
| PRC-G04 | 组内排序 | 查看组内子行 | 先按销售部门升序，再按时段升序 | P1 |

### 4.4 新增 / 编辑（单条）

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| PRC-A01 | 打开新增 | 点击「新增」 | 弹出编辑弹窗，标题为新增含义，表单为空或默认值 | P0 |
| PRC-A02 | 新增保存 | 填写物品、库存点、版本、时段、销售部门、客户、基础售价、折扣率、最终售价、货币、备注等，保存 | 弹窗关闭，列表刷新并包含新记录；若未选版本则需在表单或逻辑上关联版本 | P0 |
| PRC-A03 | 打开行编辑 | 点击某行「编辑」 | 弹出编辑弹窗，标题为「编辑售价明细」，表单回填该行数据 | P0 |
| PRC-A04 | 编辑保存 | 修改后点击「保存」 | 弹窗关闭，列表刷新，该行显示新数据，并标记为待保存（顶栏「保存」可点） | P0 |
| PRC-A05 | 取消 | 弹窗中点击「取消」或关闭 | 弹窗关闭，无变更 | P0 |

### 4.5 行内编辑（折扣率 / 最终售价）

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| PRC-IN01 | 双击折扣率 | 双击某行「折扣率」单元格 | 进入编辑态，可输入数值 | P0 |
| PRC-IN02 | 改折扣率联动 | 修改折扣率后 Enter 或失焦 | 最终售价 = 基础售价 × 折扣率（保留两位小数）；该单元格标记「更改待保存」 | P0 |
| PRC-IN03 | 双击最终售价 | 双击某行「最终售价」单元格 | 进入编辑态，可输入数值 | P0 |
| PRC-IN04 | 改最终售价联动 | 修改最终售价后 Enter 或失焦 | 折扣率 = 最终售价 / 基础售价（保留两位小数）；该单元格标记「更改待保存」 | P0 |
| PRC-IN05 | 取消编辑 | 编辑中按 Esc | 恢复原值，不标记待保存 | P0 |
| PRC-IN06 | 数值精度 | 任意联动计算 | 折扣率、最终售价及组内平均统一保留两位小数 | P0 |

### 4.6 批量编辑

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| PRC-B01 | 未选行点批量编辑 | 未勾选任何行点击「批量编辑」 | 根据实现：禁用或提示请先选择记录 | P0 |
| PRC-B02 | 跨版本多选 | 勾选属于不同财务版本的行后点击「批量编辑」 | 提示不允许跨版本多选，禁止打开批量编辑弹窗 | P0 |
| PRC-B03 | 同版本多选打开 | 勾选同版本多条记录后点击「批量编辑」 | 弹出「批量编辑售价数据」弹窗，按销售部门分组展示已选记录；组内可「统一赋值」 | P0 |
| PRC-B04 | 统一赋值 | 在某一销售部门组内对基础售价/折扣率/最终售价使用「统一赋值」 | 组内该字段统一为输入值；联动规则同行内编辑（折扣率↔最终售价） | P0 |
| PRC-B05 | 确认批量修改 | 填写后点击「确认修改」 | 弹窗关闭，数据写回，涉及单元格标记待保存，列表刷新；组内平均重新计算 | P0 |
| PRC-B06 | 父行勾选 | 勾选父行（组头） | 该组所有子行被勾选，可一起进入批量编辑（需同版本） | P0 |

### 4.7 删除

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| PRC-D01 | 行内删除 | 点击某行操作列「删除」 | 二次确认（如「确定删除该条记录？」） | P0 |
| PRC-D02 | 确认删除 | 确认后 | 该行从列表移除，提示删除成功；若有待保存则一并体现 | P0 |
| PRC-D03 | 取消删除 | 在确认弹窗取消 | 不删除 | P0 |

### 4.8 初始化

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| PRC-GN01 | 有未保存时点初始化 | 存在待保存修改时点击「初始化」 | 提示未保存的更改将丢失，是否继续；可选继续或取消 | P0 |
| PRC-GN02 | 确认初始化 | 在初始化弹窗选择版本并确认 | 按设计从版本/模板初始化售价；当前版本数据被覆盖或生成新数据，列表刷新 | P1 |
| PRC-GN03 | 取消初始化 | 在初始化弹窗点击取消或关闭 | 不执行初始化 | P0 |

### 4.9 导入 / 导出 / 视图设置

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| PRC-IM01 | 打开导入 | 点击「导入」 | 弹出导入售价数据弹窗：全量/增量、文件选择、确认/取消 | P0 |
| PRC-IM02 | 全量/增量导入 | 选择方式与文件后确认导入 | 全量清空后导入/增量追加，列表刷新，提示成功 | P0 |
| PRC-EX01 | 导出 | 点击「导出」 | 导出当前列表（含筛选、当前版本）为文件 | P0 |
| PRC-VS01 | 视图设置 | 点击「视图设置」 | 弹窗含筛选、排序、列设置三个 Tab；设置按售价模块独立保存，保存后下次进入恢复 | P0 |

---

## 五、成本明细

### 5.1 财务版本与顶栏

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| CST-V01 | 选择财务版本 | 在「财务版本」下拉选择版本 | 当前成本类型 Tab 列表立即按该版本筛选并刷新 | P0 |
| CST-V02 | 保存与待保存 | 无修改时「保存」禁用；有行内/批量修改后 | 显示「*」「（更改待保存）」且「保存」可点；保存成功后清除标记并刷新 | P0 |

### 5.2 成本类型 Tab

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| CST-T01 | 切换 Tab | 依次点击生产成本、采购成本、运输成本、库存成本、违约罚金 | 表格、列、分页、勾选状态按各自 Tab 独立；各 Tab 分页与展开状态独立保持 | P0 |
| CST-T02 | 列与字段 | 切换不同 Tab | 生产成本/采购/运输/库存/违约罚金列与设计一致（含各自成本字段、挂靠对象差异） | P0 |

### 5.3 统一筛选与「字段存在才参与」

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| CST-F01 | 筛选面板 | 点击「筛选」 | 展示：库存点代码、物品代码、生产资源代码、采购路径代码、运输路径代码、时间范围、客户ID（设计说明）、启用状态（原型）等 | P0 |
| CST-F02 | 公共条件 | 填写库存点、物品、时间范围后查询 | 所有成本类型 Tab 均按这些条件过滤 | P0 |
| CST-F03 | 生产资源代码 | 仅填「生产资源代码」后查询 | 仅生产成本 Tab 按该条件过滤；采购/运输/库存/违约罚金不受该字段影响（或该字段不存在则不参与） | P0 |
| CST-F04 | 采购路径 | 仅填「采购路径代码」 | 仅采购成本按采购路径过滤；其他 Tab 不受影响 | P0 |
| CST-F05 | 运输路径 | 仅填「运输路径代码」 | 仅运输成本按运输路径过滤 | P0 |
| CST-F06 | 客户ID | 填写客户ID（若筛选项存在） | 仅违约罚金 Tab 按客户ID过滤；其他成本类型不受影响 | P0 |
| CST-F07 | 查询与重置 | 填写条件后「查询」/「重置」 | 查询为条件交集；重置清空条件；筛选后当前 Tab 勾选清空 | P0 |

### 5.4 分组、分页、展开/折叠

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| CST-G01 | 生产成本分组 | 在生产成本 Tab 查看 | 按物品+库存点+版本分组；展开后先组内平均行再子行 | P0 |
| CST-G02 | 采购/运输分组 | 在采购、运输 Tab | 按物品+库存点+版本分组；组内平均仅含当前 Tab 成本字段 | P0 |
| CST-G03 | 库存成本分组 | 在库存成本 Tab | 按物品+库存点+版本分组（挂靠库存点物品） | P0 |
| CST-G04 | 违约罚金分组 | 在违约罚金 Tab | 按客户+版本分组；列含客户、物品、库存点等 | P0 |
| CST-G05 | 分页独立 | 在各 Tab 切换每页条数、页码 | 五个 Tab 分页互相独立，切换 Tab 后各自保持页码与每页条数 | P0 |
| CST-G06 | 全部展开/折叠 | 点击「全部展开」「全部折叠」 | 仅作用于当前 Tab 当前页的组 | P0 |
| CST-G07 | 组内排序 | 查看各 Tab 组内子行 | 生产：生产资源代码升序→时段升序；采购：供应商升序→时段升序；运输：来源库存点升序→时段升序；库存/违约罚金：时段升序 | P1 |

### 5.5 新增 / 编辑（单条）

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| CST-A01 | 新增 | 在当前 Tab 点击「新增」 | 弹出编辑弹窗，仅显示当前成本类型相关字段（版本/库存点/物品等只读或默认，成本字段可编辑） | P0 |
| CST-A02 | 生产成本字段 | 在生产成本 Tab 新增/编辑 | 含生产资源代码、单位生产成本、固定生产成本、货币、备注等 | P0 |
| CST-A03 | 采购/运输字段 | 在采购、运输 Tab | 含对应路径、单位/固定成本等 | P0 |
| CST-A04 | 库存成本字段 | 在库存成本 Tab | 含单位最大库存违反成本、库存持有成本等 | P0 |
| CST-A05 | 违约罚金字段 | 在违约罚金 Tab | 含客户、单位晚交罚金、单位欠交罚金等 | P0 |
| CST-A06 | 编辑保存 | 填写后保存 | 弹窗关闭，列表刷新，当前 Tab 数据更新；若为编辑则标记待保存 | P0 |

### 5.6 行内编辑

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| CST-IN01 | 双击成本单元格 | 双击单位/固定生产成本（或采购/运输/库存/罚金对应列） | 进入编辑态，可输入数字 | P0 |
| CST-IN02 | 保存与标记 | 编辑后 Enter/失焦 | 组内平均重新计算（保留两位小数）；单元格标记「更改待保存」；保存后清除标记 | P0 |
| CST-IN03 | 有未保存时行内编辑 | 存在待保存修改时再次双击编辑 | 根据实现：允许继续编辑或提示先保存/放弃 | P1 |
| CST-IN04 | 数值精度 | 所有成本录入与组内平均 | 统一保留两位小数 | P0 |

### 5.7 批量编辑

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| CST-B01 | 跨版本多选 | 勾选不同版本的行后点击「批量编辑」 | 提示不允许跨版本/跨成本类型混选，禁止打开弹窗 | P0 |
| CST-B02 | 同版本多选 | 同版本多条记录勾选后点击「批量编辑」 | 弹窗打开，按当前 Tab 分组：生产按生产资源、采购按采购路径、运输按运输路径、库存按库存点·物品、违约罚金按客户 | P0 |
| CST-B03 | 统一赋值开关 | 在分组卡片上开启「统一赋值」并填写某成本字段 | 组内该字段联动为同一值 | P0 |
| CST-B04 | 关闭统一赋值 | 关闭「统一赋值」 | 组内各行可独立填写 | P0 |
| CST-B05 | 确认修改 | 点击「确认修改」 | 数据写回，标记待保存，列表与组内平均刷新；各 Tab 勾选状态在 Tab 内保持 | P0 |

### 5.8 勾选与全选

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| CST-C01 | 子行勾选 | 勾选/取消某子行 | 行高亮（含冻结列）；父行若部分选中则为半选 | P0 |
| CST-C02 | 父行勾选 | 勾选父行复选框 | 该组所有子行勾选；取消则全不选 | P0 |
| CST-C03 | 全选 | 点击表头全选 | 仅当前 Tab 所有记录被选中 | P0 |
| CST-C04 | Tab 间勾选保持 | 在 Tab A 勾选部分行，切换到 Tab B 再切回 Tab A | Tab A 的勾选状态保持 | P0 |
| CST-C05 | 筛选后勾选 | 执行查询/重置 | 当前 Tab 勾选清空 | P0 |

### 5.9 删除

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| CST-D01 | 行内删除 | 点击某行「删除」 | 二次确认 | P0 |
| CST-D02 | 确认删除 | 确认后 | 该行移除，列表与组内平均更新，提示成功 | P0 |

### 5.10 初始化 / 导入 / 导出 / 视图设置

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| CST-GN01 | 有未保存点初始化 | 有待保存修改时点击「初始化」 | 提示将丢失未保存更改，是否继续 | P0 |
| CST-GN02 | 初始化弹窗 | 点击「初始化」 | 弹窗选择版本，确认后按全局参数生成/覆盖该版本成本数据 | P1 |
| CST-IM01 | 导入 | 选择全量/增量与文件后确认 | 成本数据全量或增量导入，当前 Tab 列表刷新 | P0 |
| CST-EX01 | 导出 | 点击「导出」 | 导出当前 Tab 或当前版本成本数据 | P0 |
| CST-VS01 | 视图设置 | 点击「视图设置」 | 弹窗含筛选、排序、列设置；按成本明细模块独立保存，各成本 Tab 列设置可分别适配 | P0 |

---

## 六、通用与兼容

### 6.1 弹窗与交互

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| COM-M01 | 弹窗居中 | 打开任意弹窗 | 弹窗居中，有蒙层 | P2 |
| COM-M02 | 关闭方式 | 点击取消/关闭按钮/蒙层（若支持） | 弹窗关闭，不提交 | P0 |
| COM-M03 | 轻提示 | 保存成功/失败/删除成功等 | 成功/失败/警告等轻提示，短暂自动消失 | P1 |

### 6.2 主题与表格

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| COM-U01 | 主题色 | 查看顶栏、主标签、按钮 | 主题色为浅蓝 #0284c7 系 | P2 |
| COM-U02 | 表格样式 | 表格斑马纹、悬停、选中 | 斑马纹、悬停高亮、选中态明显区分 | P2 |
| COM-U03 | 固定列 | 售价/成本表格横向滚动 | 勾选列等冻结列固定，滚动时不动 | P1 |

### 6.3 数据与边界

| 用例编号 | 测试项 | 操作步骤 | 预期结果 | 优先级 |
|----------|--------|----------|----------|--------|
| COM-D01 | 空列表 | 无数据时 | 表格展示空状态或「暂无数据」 | P1 |
| COM-D02 | 版本下拉数据源 | 在财务版本新增/编辑/删除版本后 | 售价明细、成本明细的「财务版本」下拉选项与版本列表一致（仅版本名称） | P1 |
| COM-D03 | 列宽与视图 | 在视图设置中调整列顺序/列宽/显隐后保存 | 刷新或再次进入后设置恢复，不影响分页与筛选逻辑 | P1 |

---

## 七、测试执行说明

1. **执行顺序建议**：先 P0（主流程与关键规则），再 P1，最后 P2。
2. **环境**：使用与设计一致的测试环境；导入/导出用例需准备合规的 .xlsx/.xls/.csv 文件。
3. **数据**：财务版本为售价、成本的前置数据，建议先造好版本再测售价与成本。
4. **回归**：版本/售价/成本任一模块改动后，建议至少回归该模块全部 P0 及关联用例（如版本下拉、跨版本校验等）。

---

*文档版本：1.0 | 基于设计说明与原型编写 | 覆盖财务版本、售价明细、成本明细全部用户操作*
