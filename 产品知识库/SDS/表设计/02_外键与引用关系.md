# SDS 模块 — 外键与引用关系

- **逻辑库**：`scp_sds`
- **说明**：下列为 MySQL `information_schema` 中登记的 **REFERENTIAL CONSTRAINT**；业务上大量关联未建库级外键，见同目录 `03_业务关联与 ER 说明.md`。

本库在元数据中 **未检出** 数据库级外键（或当前账号不可见）。表间关联以应用层与命名约定为主。
