# IPS运行监控中心 - 功能历史

本文件记录IPS运行监控中心PRD的所有迭代历史，每次更新须在此记录时间戳、本轮目的、与上一轮差异。

---

## 时间戳：20260310103000（第2版）

**本轮目的**：PRD结构调整 - 将评审意见整合到正文

**触发原因**：用户要求将评审意见中的技术规范整合到第2章"详细解决方案"中，作为已设计的一部分

**主要变更**：

### 1. 删除第3章"评审意见"，将内容整合到第2章

**整合方案**：
- 问题1（并发处理模式）→ 新增 2.1.6 并发控制规范
- 问题2（WebSocket推送）→ 新增 2.1.7 实时推送机制
- 问题3（终止接口与异常跳过）→ 新增 2.1.8 终止与异常跳过
- 问题4（性能监控接口）→ 新增 2.1.9 性能监控

**章节编号调整**：
- 原 2.1.6 原型图 → 调整为 2.1.10 原型图
- 原 2.1.7 测试用例 → 调整为 2.1.11 测试用例
- 第2.2章节保持不变

**文档结构变化**：
- 第1章：需求概述（保持不变）
- 第2章：详细解决方案（新增2.1.6-2.1.9四个章节，原2.1.6-2.1.7调整编号）
- 第3章：已删除

**文件规模**：968行（原1205行，删除第3章后减少237行）

### 2. 内容完整性保障

所有技术规范、框架设计、实现指导都完整保留，仅调整了章节位置和编号，使其更符合产品文档规范。

---

## 时间戳：20260310103000（第1版）

**本轮目的**：评审后补充与迭代

**触发原因**：第一次评审后的补充意见（`产品设计/产品需求/IPS/IPS_运行监控中心/第一次评审后的补充意见.txt`）

**主要变更**：

### 1. 大幅扩充第3章"评审意见"

针对4个评审问题，补充详细的技术规范和框架设计：

#### 1.1 问题1：并发处理模式的技术规范和框架

- **新增内容**：
  - 定义并发模式枚举（`ConcurrencyModeEnum`）：PARALLEL、EXCLUSIVE、QUEUEABLE、LIMITED_PARALLEL
  - 设计并发控制框架（`ConcurrencyControlManager`）
  - 详细说明各并发模式的实现逻辑、适用场景、前端交互
  - 提供死锁管理与异常处理方案
  - 提供实现指导与代码示例

- **与上一轮差异**：
  - 上一轮：仅描述Redis分布式锁的基本实现（EXCLUSIVE模式）
  - 本轮：补充多种并发模式支持、任务排队机制、统一的并发控制框架

#### 1.2 问题2：舍弃轮询采用WebSocket的技术规范和框架

- **新增内容**：
  - WebSocket推送架构设计（端点、消息推送服务、进度更新监听器）
  - WebSocket端点设计（端点路径、消息格式、消息类型枚举）
  - 消息推送服务设计（单播、广播、心跳检测）
  - 前端WebSocket客户端实现指导（连接建立、消息处理、断线重连、降级到轮询）
  - 消息可靠性保障（断线重连、消息补发、心跳检测）
  - 与轮询方式的兼容性（降级策略、配置开关）

- **与上一轮差异**：
  - 上一轮：仅提到"可考虑采用WebSocket推送"
  - 本轮：提供完整的WebSocket推送技术规范与实现指导

#### 1.3 问题3：终止接口的规范和异常跳过框架

- **新增内容**：
  - 终止接口详细规范（请求/响应格式、错误码定义、前端交互流程）
  - 异常跳过框架设计（节点枚举接口扩展、异常跳过逻辑实现）
  - 异常跳过的日志记录规范（运行日志格式、前端展示）
  - 异常跳过的配置方式（硬编码 vs 配置表动态配置）

- **与上一轮差异**：
  - 上一轮：仅描述终止接口的基本实现，提到"若业务上确有需求，应通过配置或策略控制"
  - 本轮：提供完整的异常跳过框架设计与实现指导

#### 1.4 问题4：性能监控接口的规范和设计

- **新增内容**：
  - 节点级业务指标记录规范（记录格式、命名规范、记录方式）
  - 性能监控接口设计（接口路径、请求参数、响应格式）
  - 性能数据采集方式（JVM指标、线程指标、数据库连接池指标）
  - 前端展示方式（进度弹窗中的"性能详情"按钮、性能详情对话框）
  - 后续增强方向（独立的性能监控模块、Prometheus + Grafana）

- **与上一轮差异**：
  - 上一轮：仅描述节点级业务指标的基本记录方式，提到"暂不包含系统级性能监控"
  - 本轮：提供节点级业务指标的详细规范，补充性能监控接口设计与后续增强方向

### 2. 补充评审意见总结

- 新增"3.6 评审意见总结"章节
- 提供四个问题的解决方案总结表格（解决方案、核心组件、实现优先级）
- 提供实现路线图建议（分三个阶段：P0/P1/P2）

### 3. 调研依据

- 基于 `scp-foundation` 代码仓库现状调研（详见 `产品设计/产品知识库/IPS/代码/运行监控中心_现状调研.md`）
- 重点参考MRP模块的实现经验（`ProcessProgressManager`、`ProgressNodeEnum`、`ScheduleProgressListenerController`）
- 调研了Redis分布式锁、进度推送机制、终止接口、节点级业务指标等现有实现

### 4. 文档结构变化

- 第1-2章：保持不变（需求概述、详细解决方案）
- 第3章：大幅扩充（从原来的4个简短回答，扩充为包含详细技术规范、框架设计、实现指导的完整章节）
- 新增章节：3.2-3.6（问题1-4的详细规范 + 评审意见总结）

---

## 初稿：2026-02-27

**本轮目的**：初稿完成

**主要内容**：

- 第1章：需求概述（背景及问题、目标）
- 第2章：详细解决方案（运行监控中心、新功能接入与运行节点规范）
- 第3章：评审意见（预评审问题的初步回答）

**文件路径**：`产品设计/产品PRD/IPS/IPS_运行监控中心/IPS-运行状态监控-PRD.md`

---

## 使用说明

1. **查看最新版本**：始终查看最新时间戳目录下的PRD文件
2. **对比历史版本**：通过本文件的"与上一轮差异"了解各版本变化
3. **新增迭代**：每次更新PRD时，须在本文件顶部新增一条记录，包含时间戳、本轮目的、主要变更、与上一轮差异
